<div class="container">
  <h2 class="title-lg mb">Question <%= @question.id %></h2>

  <p class="mt">
    <%= link_to 'All questions', user_path(@question.user), class: 'button button-main' %>
    for <%= link_to display_at_sign(@question.user), user_path(@question.user) %>
  </p>
</div>

<div class="container mt">
  <%= render 'question', question: @question %>
</div>
